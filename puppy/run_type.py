# 파이썬 표준 라이브러리에서 Enum(열거형)을 가져옵니다.
from enum import Enum


class RunMode(Enum):
    """
    프로그램의 실행 모드를 명확하게 정의하기 위한 열거형(Enum) 클래스입니다.

    문자열("train", "test")을 직접 사용하는 대신 이 Enum을 사용하면 다음과 같은 장점이 있습니다:
    - 오타를 방지할 수 있습니다 (예: "Train" 대신 "trainn"으로 잘못 쓰는 경우).
    - 코드의 가독성을 높여, 어떤 모드들이 있는지 명확하게 알 수 있습니다.
    - 자동완성 등 IDE의 지원을 받을 수 있습니다.
    """

    # 학습 모드를 나타냅니다. 에이전트가 과거 데이터를 통해 학습하는 단계입니다.
    Train = 0

    # 테스트 모드를 나타냅니다. 학습된 에이전트의 성능을 평가하는 단계입니다.
    Test = 1
